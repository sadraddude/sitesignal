import { LeadDiscoveryEngine } from "@/components/lead-discovery-engine"; // Adjust path if necessary

export default function DashboardSearchPage() {
  // This page now simply renders the LeadDiscoveryEngine component
  return <LeadDiscoveryEngine />;
} 